# دليل استضافة البوت (Deployment Guide) 🚀

حالياً البوت يعمل على جهازك، وإذا قمت بإيقاف تشغيله أو إغلاق اللابتوب سيتوقف البوت. لجعله يعمل 24/7، تحتاج لرفعه على "سيرفر" (Server).

بما أننا نستخدم ملفات لحفظ البيانات (`members_data.json` و `settings.json`)، فنحن بحاجة لاستضافة تسمح بحفظ الملفات بشكل دائم (Persistent Storage).

## الخيار الأفضل والأسهل: PythonAnywhere 🐍
موقع ممتاز لمشاريع بايثون، يعطيك مساحة تخزين دائمة (ملفاتك لن تضيع).

### الخطوات:
1.  **أنشئ حساب:**
    *   اذهب إلى [PythonAnywhere.com](https://www.pythonanywhere.com/).
    *   سجل حساباً جديداً (الحساب المجاني جيد، لكنه قد يوقف البوت كل فترة ويحتاج تجديد يدوي، الحساب المدفوع "Hacker" بـ 5$ شهرياً هو الأفضل ليعمل دون انقطاع).

2.  **رفع الملفات:**
    *   اذهب تبويب **Files**.
    *   ارفع الملفات التالية: `bot.py`، `requirements.txt`، `members_data.json`، `settings.json`.

3.  **تثبيت المكتبات:**
    *   افتح **Consoles** -> **Bash**.
    *   اكتب الأمر: `pip3 install --user -r requirements.txt`

4.  **تشغيل البوت:**
    *   **للحساب المدفوع:** اذهب إلى تبويب **Tasks** وأضف أمراً جديداً لتشغيل البوت بشكل دائم: `python3 /home/yourusername/bot.py`.
    *   **للحساب المجاني:** يمكنك تشغيله من الـ Console لكن سيتم إيقافه إذا أغلقت الصفحة لفترة طويلة.

---

## الخيار المتقدم: VPS (سيرفر خاص) 💻
استئجار سيرفر صغير (مثل كمبيوتر بعيد) بنظام Linux.
*   **مزودين:** DigitalOcean, Hetzner, AWS Lightsail.
*   **التكلفة:** حوالي 4-6 دولار شهرياً.
*   **الميزة:** تحكم كامل، ملفات محفوظة دائماً.

---

## ⚠️ تحذير بخصوص الاستضافات المجانية الأخرى
منصات مثل **Render** أو **Railway** (الخطط المجانية) تعتمد على "Ephemeral Filesystem".
*   هذا يعني: في كل مرة تعيد تشغيل البوت أو تقوم بتحديثه، **سيتم مسح ملف `members_data.json`** وستخسر كل بيانات الأعضاء!
*   لحل هذه المشكلة هناك، تحتاج لاستخدام "قاعدة بيانات" (Database) مثل MongoDB وهذا يتطلب تغيير الكود.

**✅ نصيحتي:** ابدأ بـ **PythonAnywhere** لأنه لا يتطلب تغيير أي شيء في الكود الحالي ويحفظ ملفاتك.
